# MongoDB Dashboard

A Flask web application (`app.py`) providing interactive dashboards and dynamic visualizations of key datasets fetched from a MongoDB cluster. Check `hourly_distribution.html` and `daily_revenue.html` for detailed insights.
